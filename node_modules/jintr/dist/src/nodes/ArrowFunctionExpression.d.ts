import Visitor from '../visitor';
import type ESTree from 'estree';
export default class ArrowFunctionExpression {
    static visit(node: ESTree.ArrowFunctionExpression, visitor: Visitor): Function;
}
